# Tramo 2026-09-16 09:08 — del aviso `nag` al lote 11

- **Inicio:** 2026-09-16 09:08 (medido con `date`).
- **Mandato del PO (A-031):** «trabaja sin parar hasta terminar los lotes 7-11 y para antes del
  12». Incluye lo que va antes en el mapa.
- **Decisión del PO sobre `4d` (A-031):** lo ya guardado mal se da por perdido; lo que se exige es
  que lo que se guarde a partir de ahora no falle nunca.
- **Paradas que siguen vigentes:** la regla de los diez (el lote 7 toca unos 168 archivos) y los
  puntos serios de la regla 30.
- **Sesiones:** reabiertas el 2026-09-16, así que ambas cuentan como compactadas. El PO puso los
  nombres a las dos.

## Orden

1. `#102` — limpieza: la guarda (dos falsos positivos y el repositorio de la guía, ADR 0016), la
   documentación de la reapertura, y la regla 30 al día.
2. El aviso de `app_key` con `nag`.
3. `4d` — el escape doble del ORM, en el paquete `database`.
4. `4e`, `4f` y `4b-4`.
5. Lotes 7, 7c, 8, 9, 10 y 11.
6. **Parar antes del 12.**

En paralelo, sin el coder: la guía personal.

## Rondas

- `#102 · ARQ` / `#103 · COD` — limpieza. **Cerrada.** `92ee7ef0` (la guarda: dos falsos
  positivos y el repositorio de la guía), `25c93d27` (regla 30, mapa y pendientes) y `e4ee12e7`
  (estado). Cuatro provocaciones, cada una con sus FALLO y ninguno más; 209/209.
  - Hallazgo del coder: el ADR 0016 promete dos provocaciones y `#102` dictó una. La que falta
    (`GUIA_PO` fuera de `ESCRIBIBLES_EXTRA`) va al principio de `#104`. No la hizo por su cuenta,
    porque añadir trabajo cambia qué se produce: aplicó bien «parar o ajustar».
  - Propuesta del coder, subida a la regla 30: al provocar la propia guarda, prueba, restauración
    y suma en una sola llamada.
- `#104`→`#111` — **el aviso de `app_key` con `nag`. Cerrado.** `226dc26b` (producto), `a35064c9`
  (artefactos de phpstan), `37c939ef` (documentación) y `7661a2c0` (estado).
  - `#105`: bloqueo por credencial, porque `#104` prohibía más de lo que permite el ADR 0010.
    `#106` autoriza restablecer `zz-prueba-root` con la comprobación de prefijo DENTRO del guion.
  - `#107`: incidente, seis filas en la base local. `#108`/`#110`: R2 con la forma verificada.
    `#111`: tabla idéntica a la de las 09:26; aviso oculto (0) y visible (1); capturas;
    limpieza de la acción temporal y de la credencial.
  - Desviación declarada y aceptada: `--ignore-certificate-errors` en Chrome, que cumple las
    cuatro condiciones de «parar o ajustar».
  - La «×» que no salía en la captura: la comprobó el arquitecto con una página mínima; era la
    fuente de iconos bloqueada en `file://`.
- `#112`/`#113` — **`4d`, parte A, cerrada.** `database`: `732248b` (`fix(orm)!`), `f7544f8`
  (`CHANGELOG`), `master` por avance rápido y `v5.0.0`. PHPStan del paquete, 18 igual; 12 suites en
  verde, con 36 comprobaciones nuevas y ninguna vieja cambiada. Provocaciones: P1 24 caídas (el escape
  se multiplica al asignar y al guardar: H-R en directo), P2 16, P3 6 y P4 4. La comprobación 17 avisa
  sin fallar.
  - Hallazgo 12.1: el `CHANGELOG` decía «SQLite en memoria» y la suite corrió contra MySQL. **Fallo del
    arquitecto**: lo escribió antes de medirlo y, al descubrirlo en `#112`, corrigió la instrucción pero
    no barrió el documento que repetía el error. Se corrige en `#114`, antes del push.
  - 12.3: el camino de reserva por SQLite no se ha ejercitado: queda SIN VERIFICAR.
  - **Lección:** al descubrir que algo escrito es falso, se barren TODOS los documentos que lo repiten,
    no solo el que se está editando.
- `#114`/`#115` — **cerrada.** `database` `4fc608d` (el `CHANGELOG` corregido, `master` adelantado,
  `v5.0.0` intacta); `piecesphp` `d9818708` (ADR 0017: P1 y P2 mordieron, 216/216) y `16ee6872`.
  - Hallazgo del coder: el ADR 0017 generaliza a «cualquier lote» una autorización del PO que era para
    `4d`. Llevado al PO en A-034 con predeterminado; el contrapeso es que cada actualización exige
    antes su push.

- `#116`/`#117`/`#118` — **`4f` cerrado.** Suite de caracterización antes de unificar (82 comprobaciones,
  suma idéntica después); `UploadedFileValidation::errors()`; tres provocaciones. PHPStan 734 por
  consolidación. `#117` corrigió un `git add` que faltaba en `#116` (fallo del arquitecto al redactar).
- **La guía del PO**, escrita entera a partir del dossier (`35500e2` en su repositorio): 14 áreas, 8
  situaciones, lo que hay que desaprender, instrumentos y referencia.
- `#119`→`#125` — **línea base 734 y lote 7, cerrados.** Commits `952eea90`, `8404e6c6`, `6a4e467c`,
  `acdb796f`, `0055b24d`, `6207cb0c` y `27dbe5b1`. PHPStan 731. Paradas del coder, todas correctas: la
  instantánea de firmas sin congelar desde `7b6209d2` (la comprobación 2 solo cierra en un sentido), la
  cota de retornos (183: murió `closedir()`), la contradicción de `#122` §3b y el `git rm` preparado antes
  de tiempo.

- `#126`→`#131` — **`7c`, parte A, cerrada.** Mailpit v1.31.1 verificado contra el `digest` de GitHub y
  arrancado con `--disable-version-check` (la comprobación de versión salía a internet: `#127`). Suite
  `core/mail-senders` 16/16. Estudio de los nueve envíos.
- **Lote 8:** propuesta de diseño del subagente de arquitectura, guardada en `estado/`. S1 y S2 verificados
  por el arquitecto: **inyección SQL y escalada de privilegios en el importador activo**. Push al PO.
- `#132`→`#138` — **corrección del importador, cerrada**: `4ec88430`, `07c40e1b`, `c9a864ca` y `280636f8`.
  `#134` paró con razón (el validador de email consultaba el DNS y todas las filas morían ahí); `#136` dio el
  rojo exacto y cazó el octavo error del arquitecto (`isEqual(null)` y la coerción entera); `#138` cerró.
- **Compactación del arquitecto** tras `#138`: resumen enviado; el coder cazó cuatro hallazgos sin registrar
  y la P29 ausente de «Espera al PO» (punto 19 de `pendientes.md`).
- `#139`→`#140` — **destinatarios de los formularios a la configuración, cerrada** (ruptura 28): `1427186e`,
  `ab42ce08`, `03c1fa7b` y `c026b971`. Arregla además el error fatal de `other-problems-send` sin osTicket.
- `#141`→`#142` — **`7c` parte B1, cerrada**: `9114ffc2` (escape de las plantillas de los formularios públicos,
  1/22 → 22/22), `54408482` (envíos 6, 8 y 9 contra Mailpit, 45/45), `f3d35240` y `6bdaa07e`. El 7 queda fuera (P30).
- **El PO empujó los cuatro paquetes** y decidió P30 («soluciona sin perder función»), P32 (documentación de
  paquetes en español, lote 9) y **P34: arquitecto y coder versionan y etiquetan el framework, salvo la MAYOR
  estable (ADR 0019)**. P31 quedó con una propuesta: Google no tiene claves de prueba para v3.
- `#143`→`#144` — **ADR 0019 y `v8.0.0-alpha.1`, cerrada**: `eca9ee29`, `53bd741e`, `54562173`, `b6c52b9f` (etiqueta
  anotada) y `e5d499bd`. `master` y `last-stable` avanzadas con `update-ref` y valor anterior. Sin empujar.
- `#145 · ARQ` — P30, recuperación de contraseña (ADR 0018). `#146 · COD`: paró en el rojo, 6/16 y no 7/16, con
  razón. `#147 · ARQ`: se acepta 6/16. `#148 · COD`: verde 16/16, pero P1 (quitar el email de `findValid`) no hacía
  caer g1. `#149 · ARQ`: g1 con un segundo usuario real. `#150 · COD`: g1 y las tres provocaciones cuadran, pero
  verify-integrity (5 firmas retiradas y una línea de comentario más) y `otp-rate-limiter` (congelaba las 4 vías)
  en rojo, fuera de lo dictado. `#151 · ARQ`: firmas retiradas a mano (sin regenerar), lista comentada fuera y cota
  del registro narrativo bajada, suite del OTP a cinco vías, PHPStan 731 → 715 con su reparto. `#152 · COD`: **cerrada**,
  `3197defd`, `b156d4bf`, `bfafc69a`, `dcdc2484` y `571b206d`. El arquitecto leyó el diff (controlador, API, vista y JS).
- `#153`→`#154` — **`v8.0.0-alpha.2`, cerrada**: `4c88c1e6`, `e9100d63` (etiquetada) y `49bc306b`. `master` → `e9100d63`.
- `#155`→`#156` — **`4d` parte B y `4e`, cerrada**: `ea153623`, `b14ea62b`, `f58fb409` y `9242cebd`. Rojo 5/25, verde
  25/25; `composer.lock` solo cambió `piecesphp/database`. Dos desviaciones de forma aceptadas (la ruta de
  `node_modules`, que el arquitecto dictó mal, y cómo se lanzó `composer`).
- `#157 · ARQ` — `4d` parte C: `bin/cli repair-escaped-text`. `#158 · COD`: paró en el rojo, 1/11 y no 0/11 (d5 solo lee). `#159 · ARQ`: se acepta 1/11 y se añade la provocación P3 con una comprobación previa de filas ajenas. *En vuelo.*

- **Incidente:** cierre de los IDE durante `#159`; sesiones reabiertas. `#160 · ARQ`: control de daños; `#161 · ARQ`: P3 en
  modo cuenta, porque había 10 filas ajenas candidatas. `#162 · COD`: **`4d` parte C cerrada**, sin daños:
  `e1ae7bb9`, `0a193252`, `7cfbdf21` y `fedff2d2`.
- `#163`→`#164` — **`v8.0.0-alpha.3`, cerrada**: `f1cf285a`, `f7efd0ef` (etiquetada) y `0d540caf`. `master` → `f7efd0ef`.
- `#165`→`#166` — **`7c` B2a, cerrada**: `4ec7d774`, `624f0aa9`, `f6bb50aa` y `42f55d8b`. Rojo 15/17 como se predijo.
- `#167`→`#168` — **`7c` B2b, cerrada**: `a0ebe0a8`, `0bd46a24` y `e470ad12`. Sin cambio de producto.
- Estudio de B2c con dos exploraciones de solo lectura; el arquitecto verificó en el código las líneas que dicta.
- `#169 · ARQ` — `7c` B2c: aprobaciones y alta por API, con el escape del nombre. `#170 · COD`: paró en el rojo, 37/41 (a4: el asunto lleva el título de la app delante). `#171 · ARQ`: a4 «termina en», título restaurado, hallazgo de `SystemApprovalsMapper::save()` sin sesión. `#172 · COD`:
  **cerrada**, `4b60281e`, `c9594221`, `95c7f829` y `b83dfe13`.
- `#173`→`#174` — **`7c` B3, cerrada** (`2b33cdaf`, `24f726a1`): tres correos reales a Mailinator, `true`. **`7c`, cerrado.**
- `#175`→`#176` — **`v8.0.0-alpha.4`, cerrada**: `c9d18b5e`, `4c928396` (etiquetada) y `41bca658`.
- Lote 9: inventario en curso.

## Hallazgos

- **Secreto expuesto (P31):** la clave secreta de reCAPTCHA v3 escrita en `GoogleReCaptchaV3Controller.php` desde
  2021. Avisado al PO por push; no se toca (regla del PO sobre secretos).
- **La recuperación de contraseña por enlace deja al usuario sin contraseña** (P30): `new-password-create` la
  cambia y la plantilla del correo no la imprime. Push al PO. El envío 7 de `7c` queda fuera hasta su decisión.
- **`AHORA.md` tenía 567 líneas y `pendientes.md` 1807.** Es justo el volumen inmanejable del que
  avisó el PO. `AHORA.md` se rehízo corto; la historia sigue en git, en el tramo del 2026-09-15 y
  en `pendientes.md`.
- **La norma «ninguna jornada termina con el árbol sucio» nunca llegó a la regla 30**, pese a que
  `AHORA.md` decía «se sube en la ronda siguiente». Subida en `#102`.
- **La guarda bloqueaba `git … branch --show-current 2>&1`**: `revisar_git` no quitaba las
  redirecciones. Es la orden que la regla 30 dicta para el PASO 0 de los paquetes, en cuanto se le
  añade una redirección.
- **La guarda bloqueaba `python3 -m pip list`** como si fuera una instalación.
- **La guarda bloqueó el propio arreglo**: un heredoc que CONTIENE el texto de una orden prohibida
  se analiza línea a línea como comandos. Es un falso positivo asumido por diseño; se resolvió
  escribiendo el parche en un archivo del scratchpad y ejecutándolo desde ahí.

## Decisiones

- **ADR 0019** (PO): versionado y etiquetas del framework para arquitecto y coder, salvo la MAYOR estable. La
  guarda solo deja pre-versiones y `update-ref` con valor anterior (233/233). El `CHANGELOG` no se reparte por
  pre-versión: la sección de la MAYOR lleva una tabla de pre-versiones.
- **P30 va antes que `4d` parte B**: es una toma de cuenta posible en rutas públicas.

- **`7c`, la capa 1 del mapa («composición sin red») queda absorbida en la 2** (Mailpit, que es local): la
  composición se comprueba en el mensaje recibido. Solo el escape de las plantillas se prueba renderizando,
  porque es una guarda y tiene que poder provocarse sin enviar nada.

- ADR 0016: la guía del PO vive en su repositorio; el arquitecto la escribe y la commitea.
- Regla 30: parar o ajustar (`#100`), la norma del árbol limpio y el límite de la línea de
  compactación.

## Fallos y cómo se resolvieron

- A-030 llevaba las dos órdenes de renombrado dentro de un único bloque mezclado con el resto, y
  el PO no las vio. En A-031 fueron por separado, una por bloque.

- **`#106`: el arquitecto dictó como segura una forma de POST que era una lectura del coder, sin
  verificarla.** El coder había escrito «lo he leído en el código, no lo he probado»; el arquitecto
  lo convirtió en orden sin abrir `Parameters::validate()`. Resultado (`#107`): seis filas nuevas en
  `pcsphp_app_config` de la base local desechable, ninguna existente alterada. Lo cazó el
  discriminante que el propio arquitecto añadió. Se deshace con R2 en `#108`.
  - **Lección:** una lectura ajena de código no entra en una instrucción como hecho. Se abre el
    archivo, o se dicta como SIN VERIFICAR con su discriminante. Aquí el discriminante existía, y
    por eso el daño quedó en seis filas.

- **`#108`: el arquitecto dictó `AppConfigModel::delete()` sin comprobar que existía**, justo después
  de anotar la lección anterior. No existe (`#109`). Esta vez la condición de parada estaba escrita,
  y el coder paró antes de escribir. En `#110` se aprobó su forma (iii), que el arquitecto verificó en
  el código instalado antes de dictar: `getModel()->delete(new WhereSegment([WhereItem::isEqual(...)]))`,
  ligada por marcador.

- **`#117` y `#120`: dos correcciones de instrucciones ya enviadas**, las dos por errores del arquitecto al
  redactar: un `git add` que faltaba y una cifra de censo mal contada (12 en vez de 14). Se cazaron al
  releer lo enviado, antes de que el coder llegara a ese paso. **Lección:** releer cada instrucción
  contra las mediciones propias antes de enviarla, no después.

- **`#128` → `#129`: sexto fallo de redacción del arquitecto en el tramo.** Dictó que `/api/v1/info` dejaría
  de traer `LatestVersion`, sin medirlo; Mailpit lo rellena con `"disabled"`. El coder paró con el criterio
  literal. **Lección sobre herramientas externas:** su comportamiento no se dicta como hecho; se dicta
  «mide y reporta», o se comprueba antes en la documentación o con una ejecución propia.

- **`#132` → `#137`: octavo fallo de redacción del arquitecto.** Dictó `WhereItem::isEqual('id', …)` con un
  valor que podía ser nulo y una cadena ligada contra una columna entera; ninguna de las dos formas es segura
  (`#136`). Se aprobó el `getByID()` del coder, que valida el dominio antes de consultar.

- **El arquitecto se compactó y dejó hallazgos sin registrar.** Los tenía «para registrar» en su memoria de
  trabajo y no en `pendientes.md`. El contraste del resumen por el coder los recuperó. **Lección:** un
  hallazgo se escribe en el registro en el turno en que se acepta, no al cerrar la ronda.

- **El borrador de `#145` tenía dos errores que el arquitecto cazó al releerlo contra el código**, antes de
  enviarlo: el rojo esperado ignoraba que las rutas exigen el número exacto de parámetros (un `username` de más da
  `MISSING_OR_UNEXPECTED_PARAMS`, y varias comprobaciones pasan por esa razón), y dictaba `git rm` de plantillas
  después de la verificación, cuando borrarlas cambia el universo de PHPStan. **Lección, otra vez:** releer la
  instrucción contra el código antes de enviar, y razonar el rojo caso a caso.

- **`#145`: noveno error de redacción del arquitecto, cazado por el rojo razonado.** Predijo g5 en verde porque la
  respuesta de error no llevaría `user`; `newPasswordCreateCode` inicia `$json_response` con `'user' => null`
  (`RecoveryPasswordController.php:359`), así que la clave está en todas las respuestas. El arquitecto había leído esas
  líneas. El coder paró con el criterio literal y explicó la causa. Sin daño: nada commiteado.

- **`#145`: décimo error del arquitecto, de diseño de prueba.** g1 usaba un nombre que no es usuario, así que el
  controlador ni llamaba a `findValid()`: la ligadura del código al usuario quedaba sin prueba (LEY 24). Lo cazó la
  provocación P1 del coder, que propuso un segundo usuario real. **Lección:** para probar que algo no vale para OTRO,
  el otro tiene que existir; si no, se prueba una guarda anterior.

- **`#145` no previó tres efectos medibles antes de enviarla** (undécimo fallo del arquitecto): las firmas de los
  métodos retirados (comprobación 2), la suite que congela `OTPRateLimiter::VIAS` y el registro de comentarios
  narrativos. El coder paró en los tres. **Lección:** al retirar métodos o ampliar una constante, se buscan antes
  en `files/dev/integrity-signatures.json` y con `git grep` en `local-tests/`.

- **`#157`: decimotercer fallo del arquitecto**, otra vez el rojo: d5 solo lee, así que pasa sin la clase; y ninguna
  provocación la hacía caer. El coder lo explicó y propuso la provocación que faltaba. Además, el INSERT dictado no
  llevaba `date`, que la columna exige. **Lección, repetida:** cada comprobación del rojo se razona contra «¿llama a lo
  que no existe?», y cada comprobación del verde necesita una provocación que la haga caer.

- **`#169`: decimocuarto error del arquitecto**: dictó que el asunto «empieza por» Aprobaciones sin medir `get_title(true)`, que antepone el título de la app. El rojo razonado lo cazó.

- **P29 escrita sin medir, y retirada antes de llegar al PO.** El arquitecto registró `Validator::isEmail()`
  como núcleo transversal «que usan todos los formularios». Al revisar esa frase para la guía, lo midió: su
  único llamador es el importador de usuarios. **Lección:** «transversal» es una afirmación de uso, y se mide
  con `git grep` antes de escribirla.

## Espera al PO

Ver `AHORA.md`, «Espera al PO».

## Resumen

*Se rellena al cerrar el tramo.*

### Ronda 9.1 (#177-#181, 2026-09-16 13:24)

- #177/#178: docs de contradicciones del dossier, processFromQuery, plan del lote 9 (3621c398, a8cbe8fa, ce45b751).
- #179/#180: comentarios de `bin/cli` y `GatesTask`, CHANGELOG (errata «# Eliminaciones», un solo uso de `escapeString()`) (85de2671, 0a5faab9).
- #181: selectores sin productor en `09-frontend-assets.md`, `historico/README.md` según ADR 0002, regla dura 11 de marcadores.
- Decidido: `@deprecated` de `escapeString()` espera a que muera su último uso (núcleo transversal y línea base).
- Nota del coder: la línea del docblock de GatesTask quedó en ~160 columnas; cosmético, sin ronda.

### Ronda 9.2 (#183, 2026-09-16 13:27)

- `ignoreErrors` en dos listas; bloque de deadcode mixto sin partir (delimitadores). 166 líneas de regla idénticas como multiconjunto.
- Hallazgo al lote 10: la pasada 8.4 de `bin/phpstan` sobra con `>=8.5 <8.6`.

- #184 bloqueó: el reorden cambiaba qué patrón absorbe cada error (6 -> 9 sin casar, total igual). #185 rehecha conservando el orden; #186 cerrada, PHPStanResult.txt idéntico byte a byte.

### Ronda 9.3 (#187, 2026-09-16 13:37)

- API: Reportes.md reescrito contra `ReportsManageQueries` (no hay «convocatorias»), Traducciones (translateGroup, saveGroup 410), CronJobs (falla cerrada, cabecera), índice con banderas y `external` apagada (P35, predeterminado).
- Postman: carpetas News (nueva) y Users (las 7 sueltas); translateGroup añadido.
- Hallazgo al lote 10: `API_CRONJOBS` sola no registra rutas.
