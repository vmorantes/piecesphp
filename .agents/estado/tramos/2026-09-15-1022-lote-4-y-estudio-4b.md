# Tramo 2026-09-15 10:22 — Lote 4, bloque 2, y el estudio del 4b

- **Inicio:** 2026-09-15 10:22 (medido con `date`)
- **Fin:** abierto
- **Mensajes:** desde `#040`
- **Mandato del PO:** «Creo que ya no tengo nada por decir. Trabajen.», al final de su repaso de
  los commits del día anterior. Trabajo nombrado: el mapa, en su orden.

## Antes de la primera ronda: el repaso del PO (2026-09-15)

Decisiones y encargos de la conversación, todos en `docs/pendientes.md`:
- El andamiaje muerto se retira si Publications ya enseña el patrón.
- Publications es arquetipo por su recorrido; una propuesta mejor se acepta.
- **El lote 4b, rendimiento de los archivos protegidos.**
  - Decide el NOMBRE del archivo (el sufijo `.protected`), porque bajo HestiaCP Nginx no lee el
    `.htaccess`.
  - El framework es 100 % autocontenido.
  - La carpeta `uploads` entera la gobierna un subsistema en su propia carpeta de `Core/`.
  - Todo documentado para las máquinas y para quien desarrolle, y configurable.
- **Revisión de `ServerStatics.php`:**
  - ⚠ lo protegido sale con `Cache-Control: public`;
  - no hay ningún `Vary`;
  - carga el archivo entero en memoria;
  - convierte a WebP en cada petición.
- **FileManager (P26):** los archivos del editor irían a la carpeta de su registro y heredarían
  su visibilidad. Queda por estudiar la vista previa y los renombrados.

## Rondas

| # | Qué | Resultado | Commits |
| --- | --- | --- | --- |
| #040 / #041 | Lote 4, bloque 2: la familia de las etiquetas en literal JSON del `SELECT` (6 mappers), con un ayudante hexadecimal; el plan de los 14 llamadores de `process()`, medido; y el estudio del 4b (elFinder y `ServerStatics`), en solo lectura | **Completado.** `sqlStringLiteral()` en 11 etiquetas; 121/121; PHPStan 737. Plan de `process()`: el buscador va por `escapeString()` en 19 de 21. Estudio del 4b hecho. **H1: cualquier usuario con sesión reescribe cualquier traducción** | `7e855d26` `6a11ac0d` `c250c2ee` `4b3d3d58` |

| #042 → #043 / #044 | El buscador de `process()` (opción B), RETIRADO por el arquitecto: tocaba el núcleo transversal sin haberlo hablado con el PO. Solo se hizo T1; T2 llegó a medir la equivalencia (las mismas filas en 3 tablas) | T1 commiteada | `b52172f8` `dd07e06e` |
| #045 | Lote 4, bloque 3: el buscador de `process()` con la opción B, aprobada por el PO; equivalencia en los 19 listados; pruebas de punta a punta con navegador simulado (ADR 0010) | en vuelo | — |

## Encontrado y decidido

- **Nueva regla del PO (2026-09-15):** los cambios grandes que tocan elementos transversales del
  núcleo se hablan antes con él. Queda en la regla 30, puntos serios. El arquitecto había enviado
  `#042` sin esa conversación, y la retiró.
- **`__()` genera HTML a propósito:** hay traducciones en archivos `.html`, cargadas por
  `LangInjector`, y 15 valores con etiquetas. El `strip_tags` que había propuesto el arquitecto
  para P28 era un error, y se retiró.
- **El PO autoriza las pruebas contra la aplicación local** (ADR 0010) y aprueba aplicar las
  mejoras: la opción B para `process()`.

- **La familia de las etiquetas en literal JSON.** `JSON_EXTRACT('{json}', …) AS statusText`
  aparece en seis mappers:
  - OrganizationMapper, cuatro veces, con `escapeString()`;
  - Users, Banner, News, SystemApprovals y Publications, **sin escapar nada**.
  Una etiqueta con apóstrofo rompe la consulta. Si las traducciones se editan desde el panel,
  sería una inyección de segundo orden (SIN VERIFICAR). Los alias solo se muestran; ninguno se
  usa en `HAVING` ni en `ORDER BY`.
  **Decisión:** un ayudante que emita el literal en hexadecimal (`CONVERT(X'…' USING utf8mb4)`),
  independiente de `sql_mode`. Uniforme para los seis.

## Falló por el camino

—

## Espera al PO

- P26 (FileManager), P25 (candidata) y Locations: con predeterminado.
- El plan de los 14 llamadores de `process()`: se le presenta al recibir `#041`.

## Resumen

*(al cerrar)*
