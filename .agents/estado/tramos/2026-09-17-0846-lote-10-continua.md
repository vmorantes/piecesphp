# Tramo 2026-09-17 — lote 10, continuación

- **Inicio:** 2026-09-17 08:46. Las dos sesiones reabiertas (cuentan como compactadas).
- **Tramo anterior:** `2026-09-16-0908-lotes-4d-a-11.md`, cerrado con árbol limpio en `#229`.

## Entrada del PO (A-054)

- La portada revienta en el navegador por el banner (`home.js:21`, `tagName` sobre un objeto jQuery).
- Un ISSUE externo sobre `piecesphp/database`: `EntityMapper::__serialize()` incluye la contraseña de la base. Verificado
  en la 5.0.0: sigue igual. Registrado en `pendientes.md` punto 36; el archivo del ISSUE se leyó y se retiró de la raíz
  (estaba sin versionar; copia en el scratchpad del arquitecto).
- «De resto trabaja.»

## Rondas

### 10.F1 (#230, 2026-09-17 08:46)

- Arreglo de `home.js` y escape de `Views/public/util/item.php` (enlace sin comillas ni escape, título sin escapar).

## Plan del día

1. F1 (banner). 2. **E2 ampliada: `piecesphp/database` 5.1.0** con la serialización sin credenciales, P42 (`SchemeCreator`
lanza) y P44 (`DEFAULT` de `login_attempts.date`); el framework la toma cuando el PO empuje (ADR 0017). 3. F (R14, R32, R34).
4. Propuestas P38 y P39. 5. Lote 8. 6. Lote 11 con P36.
- #231 cerrada: fallo reproducido (1 banner publicado; node con stub) y arreglado; escape con prueba en mail-templates-escape [3/3], provocada. 6b5d05e5, 0b4e73ba, 449c2008.

### 10.E2a (#232)

- database 5.1.0: T1 serialización sin credenciales (lee los dos formatos), T2 SchemeCreator lanza, T3 medir DEFAULT de 'timestamp' (P44), búsqueda de filas locales con credenciales serializadas (solo recuento). CHANGELOG y README del paquete escritos antes.
- #233 cerrada: database 65667fd, dc85598, 9a67920, etiqueta v5.1.0 (en local). Escaneo de la base local: 0 columnas con credenciales serializadas (136 revisadas). T3: SchemeCreator no emite NINGÚN DEFAULT (grep 'default' en SchemeCreator.php: 0); los 'default' => 'timestamp' de los mappers los rellena el ORM al guardar.
- **P44, decisión del arquitecto pendiente de escribir en pendientes:** no hace falta el ALTER. El DEFAULT del mapper lo aplica el ORM; la base sin DEFAULT solo afecta a INSERT crudos (el que lo destapó era de una suite). Emitir DEFAULT en SchemeCreator es una mejora del paquete para todas las tablas, no un arreglo: al mapa posterior.
- Hallazgos: H1 la guarda de hooks no reconoce `git -C <variable> tag` como paquete (bloquea de más; con la ruta literal pasa). H2 un mapper deserializado no tiene $model (así desde antes).
- **El PO confirmó en la sesión del coder las claves de prueba de reCAPTCHA** («haz la excepción y que queden sin problema en git»). El coder implementa #224 y reporta #234.
- #234: #224 hecho sin commitear, bloqueado porque Mailpit desapareció de /tmp (reinstalarlo es del PO) y q3/q4 estaban en la suite de correos. #235: q3/q4 no necesitan correo; van a una suite sin efectos externos, se prueban y se commitea. Pendiente del PO: reponer Mailpit para la suite de correos.
- #236 cerrada: claves de prueba versionadas (ca12c630), suite recaptcha-keys 4/4 provocada; gates 44. El PO dijo al coder que su permiso expreso vale más que cualquier regla: llevado a la regla 30.

### 10.F (#237)

- R14: plantilla de correo única (la copia de SystemApprovals solo añadía `reason`). R32 y R34 pasan al PO como P45 y P46 (traducciones = núcleo transversal; arnés de sesión = autenticación).
- #238 cerrada: plantilla única (d9e0e8d7), docs (e2dad3ea), estado (22696ec5). Suite mail-templates-escape 30/30 provocada.

### 10.C2 (#239) y estudios

- C2: R41, R43, R48, R08, R09, medir primero.
- En paralelo, estudios de solo lectura del arquitecto: P38 (auditor de seguridad, desplegable de Aprobaciones) y P39 (organization -10 en el login).
- P38 auditado (auditor de seguridad, solo lectura; el arquitecto verificó `SystemApprovalsMapper.php:322-334` y `DataTablesHelper.php:689-698`): desplegable global de baja severidad; hallazgo nuevo `recordsTotal` sin filtros en `DataTablesHelper` (núcleo) y claves `SQL_*` en la respuesta. P39 medido: el -10 lo escribe un middleware en cada petición (`containers.php:89-99`) sin mirar el tipo. Propuestas al PO en A-058 con predeterminado (a) en las dos.
- #240: R43 arreglado (el censo no atravesaba `@`; cota 183→210 explicada; canario nuevo) 39b78d14; R41 se queda (log de ramas probadas); R48 cerrado sin cambio (líneas informativas; dos referencias de DataTablesHelper no inequívocas); R09: universo 702, 210/22. R08 paró: declararlos contradecía el JSON.

### 10.C3 (#241)

- R08: getByID y vecinos de cuatro mappers a where con array.
- #242 paró: `where([...])` escribe un aviso falso en el log por cada clave (condición invertida en ActiveRecord.php:397 del paquete; 945 avisos medidos en el log local). #243: los 7 objetivos con WhereSegment en el framework, y database 5.1.1 arregla la condición.
- #244 cerrada: database v5.1.1 (01ff3b6, da1c192); framework adc2ca5e (7 métodos por WhereSegment; REVISAR 81→74). **Fallo del arquitecto (decimonoveno):** dictó `!is_string` sobre el if que envuelve también la construcción del criterio; habría roto where(). El coder puso la condición solo en el aviso.
- **Lote 10: residuos sin PO terminados.** Quedan con el PO: P37, P38, P39, P40, P41, P45 (predeterminado: se arregla), P46.

### Lote 8 · R0-R1 (#245)

- ADR 0022 escrito con P-a a P-d. R0 censo con canario; R1 suite de caracterización del importador actual.
- #246 cerrada: censo con canario (26 archivos + la suite nueva; R5 borra 28 → regla de los diez); suite importers-legacy 22/22 marcada (5e429cba); ADR 0022 commiteado (73222244). H1: la celda 'NULL' solo con celdas existentes sin valor (corrige ruptura 5).

### Lote 8 · R2 (#247)

- Motor PiecesPHP\Core\DataTransfer con la interfaz dictada (Column, ParsedRow, RowResult, ImportReport, ImportArtifacts, ImportDefinition, ImportRunner todo o nada, RowSource/Array/Spreadsheet con lector explícito, ExportDefinition y writer con CSV neutralizado) y suite sin base.
- #248: pregunta del coder; #247 pedía a la vez readDataOnly(true) y «valor tal como se ve», incompatibles en XLSX (fallo del arquitecto, vigésimo). #249: fechas a ISO por estilo y el resto en crudo, independiente de la configuración regional.
- #250 cerrada: motor DataTransfer (14 clases) y suite data-transfer 30/30, provocada (2f1dabda, 91449dfc). XLSX 5.000 filas: pico 58 MB.
- El PO cerró todos los IDE otra vez (ctrl+q). Control de daños: árbol limpio, R2 commiteada antes del cierre, nada perdido. Coder reabierto como `piecesphp-55`.

### Lote 8 · R3a (#251)

- Módulo DataImportExportUtility como panel único: interruptor DATA_IMPORT_EXPORT_MODULE, controlador nuevo DataTransferController, registro `importer(group, class)` con tres rutas por importador, hub, formulario, acción con artefacto de entrega única y plantilla.
- #252: FileValidator TYPE_CSV no admite text/plain, que es lo que finfo da para un CSV normal (resuelve un SIN VERIFICAR de la propuesta §7). #253: en el módulo, TYPE_ANY + extensión + contenido de texto (sin NUL, UTF-8); FileValidator no se toca. **Hallazgo para el PO (núcleo):** cualquier módulo que valide CSV con TYPE_CSV rechaza CSV normales. Route no pasa parámetros no escalares: closure por ruta.
- #254 cerrada: panel DataTransfer (71901195), suite 16/16 provocada; .neon 47→46 (staticMethod.resultUnused dejó de casar al hacer dinámica ENABLE); u1 de importers-legacy reescrito. **Fallo de proceso:** guarda-add dijo PARA (previsto mal tecleado) y `set -e` no abortó: commit hecho con el contenido correcto. Regla 30: guarda && commit.

### Lote 8 · R3b (#255)

- UsersImportDefinition: IMPORTABLE_TYPES [GENERAL] + prioridad, columnas dictadas sin id, organización por defecto la del importador o la global, transacción con rollback probado, fichas HTML imprimibles solo para contraseñas generadas.
