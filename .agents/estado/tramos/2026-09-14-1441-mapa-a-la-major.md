# Tramo 2026-09-14 14:41 — El mapa a la MAJOR

- **Inicio:** 2026-09-14 14:41 (medido con `date`)
- **Fin:** abierto
- **Mensajes:** desde `#016`
- **Mandato del PO:** «Trabaja. Adelante.» (2026-09-14). Es la orden de trabajar sobre el
  framework siguiendo `.agents/docs/roadmap.md`.

## Rondas

| # | Qué | Resultado | Commits |
| --- | --- | --- | --- |
| #016 / #017 | Lote 1, BD: PHPStan 2.2.12 en los cuatro paquetes | **Parcial.** datastructures y geojson, sin cambios de cifra (0). database baja de 21 a 18 por el analizador y queda sin commit. html, bloqueado en Composer (P23). Registro sin tocar: la comprobación 7 queda en rojo | `11fb9d06`, `b0fdbcc3`, `7332f4b9` |
| #018 / #019 | BD, cierre: trinquete de los paquetes nivelado con el de piecesphp, línea base de database a 18 con «3 murieron», registro con html pendiente | **Completado.** verify-integrity sin fallos; gates 25/0; la provocación del trinquete, vista fallar. Bitácora 0005 | piecesphp `ca82802d` `4fc682ea` `43311b58` `7b45c761`; database `edacf6c` `e3e55d1`; datastructures `c12c067`; geojson `7a5ca38`; html `68aca08` |

| #020 | Lote 2, bloque 1: `bin/censo-sql-identificadores`, que mide las posiciones de identificador sin censar. Sin trinquete ni arreglos | en vuelo | — |

**Interrupción, a las 15:18.** Los IDE del PO se cerraron y las dos sesiones se reabrieron. No
se perdió nada: el estado estaba en disco y cuadraba con lo último conocido. El coder no tenía
ningún mensaje sin contestar.

## Encontrado y decidido

- **BD, medido antes de instruir.**
  - Los paquetes miden con PHPStan 2.1.42 o 2.1.44, que les llega de `require-dev` con `^2.1`.
  - Su `composer.lock` no se versiona.
  - `piecesphp` mide con 2.2.12, y las versiones declaradas están en
    `files/dev/shared-toolchain.json`.
- **ADR 0007.** La guarda bloqueaba todo `composer update`. Ahora deja actualizar las tres
  herramientas de análisis, nombradas una a una y con versión fija
  (`phpstan/phpstan:2.2.12`), por la delegación del PO del 2026-09-02. Además se cerró un
  hueco: la guarda no veía `php8.5 /usr/bin/composer …`. Van 166 casos de prueba.
- **Los `04-desarrollo.md` de los cuatro paquetes son el mismo archivo**, el de `database`.
  Se corrigió la regla 30, que los daba como fuente de verificación, y se anotó en
  `pendientes.md`.
- **El mapa** decía que BD se hacía en `bin/tools` de cada paquete; se hace en su raíz.
  Corregido.
- **Lote 2, cómo se aborda.** Primero el instrumento, luego la cifra, luego el arreglo: es el
  orden que dejó escrito el 20 §7, en el bloque AN. Medido antes de decidir:
  - las opciones de `DataTablesHelper` solo validan el tipo;
  - la dirección de `custom_order` entra sin filtrar (`generateOrderBy()`);
  - los 15 que pasan `custom_order` lo hacen con una variable;
  - `select`, `get`, `rowCount` y `setTable` interpolan por contrato y admiten expresiones, así
    que una lista blanca por forma rompería usos legítimos;
  - una lista blanca sacada de esta instalación es falsa para los clones (20 §7, «`region`»).
  Esas familias viven en el paquete database.

## Falló por el camino

- **La guarda bloqueaba un `composer update` legítimo** con redirección (`> f 2>&1`): tomaba el
  destino por un paquete (H1 de `#017`). Arreglado con `sin_redirecciones()`. 170 casos.
- **La condición del PASO 3 de `#016` no se cumplió.** No se había previsto que el analizador
  bajara una cifra, ni que el lock local de html estuviera desfasado. El coder paró como se le
  pedía.

## Espera al PO

- **P22**, sin bloquear: `TokenModel` y su clave.
- **Subir** los commits y las ramas `dev`.

## Resumen

*(al cerrar)*
