{
    "why": "Llamadas que CONCATENAN un valor de la petición y NO se pueden parametrizar, porque la comparación es de LISTA. `WhereItem::toString()` imprime el valor en crudo para `IN`, `NOT IN` y `FIND_IN_SET` —esos tres operadores están en `NOT_ALIAS_OPERATORS`, así que ni siquiera se les genera marcador—. Donde la vía preparada no existe, se VALIDA EL DOMINIO, que es lo que el propio módulo ya hacía con `state` y `country`. Aquí se declara cada una con la validación que la cierra. Ver T152.",
    "rules": [
        "LA LISTA SOLO PUEDE ENCOGER. El día que la biblioteca parametrice `IN`, estas entradas se retiran y el código pasa a marcador.",
        "Declarar NO es eximir de pensar: cada entrada dice QUÉ validación cierra el agujero. Una entrada sin validación descrita no vale.",
        "Se declara por `archivo::metodo`. El censo las resta de CONFIRMADO y las imprime aparte: no desaparecen, se separan.",
        "Una entrada que ya no case con ningún hallazgo hace fallar: encoger incluye vaciarse.",
        "CADA ENTRADA FIJA SU `count`. Declarar es por `archivo::metodo`, asi que sin esto una concatenacion NUEVA en un metodo ya declarado entraria gratis y el trinquete de CONFIRMADO no la veria. Se descubrio provocando en AM: al romper el `having` de `searchDropdown`, DECLARADO subio de 4 a 5 en vez de subir CONFIRMADO.",
        "NO SE DECLARA UN METODO QUE AUN TENGA UN HALLAZGO SIN CERRAR. Se declara por `archivo::metodo` con un `count`, y en un metodo con dos hallazgos el `count` no puede decir CUAL de los dos indulta: el orden decidiria. `SystemApprovalsController::dataTables` se queda entero en CONFIRMADO hasta que `referenceAlias` tenga su lista blanca."
    ],
    "medido": {
        "fecha": "2026-08-31",
        "bloque": "AL; ampliado en AM; novena familia declarada en AO",
        "hueco_de_la_biblioteca": "src/vendor/piecesphp/database/.../Critery/WhereItem.php:238-241 — `IN`/`NOT IN` imprimen `{$rightMember}` en crudo y `FIND_IN_SET` lo sustituye en `{SEARCH}`. El paquete está versionado y etiquetado: se anota, no se toca.",
        "ids_antes": "`ids[]=abc` daba 500 con `Unknown column 'abc' in 'WHERE'`",
        "ids_despues": "`ids[]=abc` da 200 y el criterio NO se añade; `ids[]=1&ids[]=abc` devuelve solo el 1",
        "not_in_dentro_de_segmento": "AM: envolver el `NOT IN` en un `WhereSegment` NO lo prepara, y el censo lo daba por limpio. Corregido: un segmento que nombre `IN`/`NOT IN`/`FIND_IN_SET` —siguiendo la cadena de variables, no solo el texto del argumento— pierde el atajo. Sin esa corrección, arreglar `UsersController::searchDropdown` habría BORRADO su hallazgo en vez de declararlo.",
        "fragmentos_de_datatables": "AO: `where_string`, `having_string` y `group_string` son FRAGMENTOS DE SQL por diseno: `DataTablesHelper::process()` los interpola con `\"($where_string) AND $where\"` y NO existe via preparada para ellos —`havingReplacePrepareValues` solo se rellena desde un `HavingSegment`, ActiveRecord.php:491 y 556—. Las que validan su dominio se declaran aqui; las que no, se arreglaron o siguen abiertas.",
        "migracion_a_segmento": "AO declaro `Country::countriesDataTables`; AP la MIGRO a `where_segment` y la entrada se retira. Ese es el ciclo previsto: se declara mientras se concatena, y la declaracion DESAPARECE cuando el valor pasa a marcador. La lista solo puede encoger, y encoger incluye por migracion, no solo por borrado. AP+AQ: migradas CUATRO —`Country::countriesDataTables`, `State::statesDataTables`, `UsersController::dataTablesRequestUsers` y `DocumentsController::dataTablesExplorer`— y sus cuatro entradas retiradas. Las tres de AQ las cazo el trinquete al quedar huerfanas, no la memoria del CODER."
    },
    "entries": [
        {
            "where": "src/app/classes/App/Locations/Controllers/City.php::cities",
            "validation": "`ids` pasa por `array_map('intval')` y se descartan los `<= 0`; si la lista queda vacía NO se añade el criterio, porque `IN ()` no compila. `state` y `country` ya eran enteros por `Validator::isInteger`.",
            "count": 1
        },
        {
            "where": "src/app/classes/App/Locations/Controllers/State.php::states",
            "validation": "`ids` igual que en City. `country` ya era entero por `Validator::isInteger`.",
            "count": 1
        },
        {
            "where": "src/app/classes/App/Locations/Controllers/Country.php::countries",
            "validation": "`ids` igual que en City. `region` son NOMBRES, así que `intval` no aplica: cada elemento tiene que casar `^[\\p{L}\\p{N} \\-]{1,60}$` y lo que no case se descarta. `UPPER('NONE')` entra siempre, así que la lista nunca queda vacía.",
            "count": 1
        },
        {
            "where": "src/app/controller/UsersController.php::searchDropdown",
            "validation": "`ignoreTypes` pasa por `is_numeric` ANTES de `intval` —porque `intval('abc')` da 0 y 0 es `TYPE_USER_ROOT`— y luego se queda solo con lo que esté en `array_keys(UsersModel::TYPES_USER_PRIORITY)`, que es el único constante del modelo que enumera los SIETE tipos declarados. `TYPES_USERS` NO sirve: es un mapa de etiquetas y trae `TYPE_USER_GOOGLE_PLAY` comentado, así que descartaría un tipo real y el desplegable mostraría MÁS usuarios. Lista vacía = no se añade el criterio.",
            "count": 1
        },
        {
            "where": "src/app/classes/Organizations/Controllers/OrganizationsController.php::dataTables",
            "validation": "`status` pasa por `in_array($status, array_keys(OrganizationMapper::STATUSES)) ? $status : -1` en `OrganizationsController.php:1206`. Es LISTA BLANCA sobre el conjunto declarado de estados; lo que no este en el se convierte en -1, que no casa nada.",
            "count": 1
        },
        {
            "where": "src/app/classes/Publications/Controllers/PublicationsController.php::dataTables",
            "validation": "`visibility` pasa por `Validator::isInteger` MAS `array_key_exists((int) $visibility, PublicationMapper::VISIBILITIES)` en `PublicationsController.php:1233-1238`. Es LISTA BLANCA sobre las cuatro visibilidades declaradas, no un entero suelto.",
            "count": 1
        },
        {
            "where": "src/app/core/psr4/PiecesPHP/Core/Utilities/Helpers/DataTablesHelper.php::process",
            "validation": "LOS CINCO son la VIA DE CADENA del helper, y las tres entradas de peticion que los alcanzan estan cerradas o acotadas, LEIDAS y no supuestas: (1) `order` no llega nunca al SQL —su indice se usa como CLAVE de `columns_order` (linea 1223) y la direccion colapsa a un ternario `ASC`/`DESC` (1222)—; (2) `columns` solo elige el indicador `searchable` entre columnas que pone la controladora; (3) el VALOR de busqueda pasa por `escapeString()` —`addslashes(stripslashes())`, AppHelpers.php:2805—, que se sostiene con `utf8mb4` pero NO con `NO_BACKSLASH_ESCAPES`, y eso esta escrito en T155. `where_string` y `having_string` son fragmentos del programador, no del visitante, y el docblock lo dice.",
            "count": 5,
            "salida": "Cada controladora que migre a `where_segment` deja de aportar su fragmento. Cuando las 19 hayan migrado, la via de cadena se puede retirar y estas cinco desaparecen. `generateHaving` no puede devolver segmento hoy sin cambiar el SQL de las 18 que no han migrado: ver la PARADA del paso 3 en T156."
        },
        {
            "where": "src/app/classes/SystemApprovals/Controllers/SystemApprovalsController.php::dataTables",
            "validation": "`elapsedDays` pasa por `Validator::isInteger($elapsedDaysFilter) ? (int) : null` en `SystemApprovalsController.php:441`, cerrado desde AO. Es el UNICO valor de peticion que queda en el `having_string` de este metodo: `referenceAlias` migro a `where_segment` en AW y va por marcador. NO se migra el `having`: `columns_order` son columnas buscables reales —`referenceAlias`, `referenceDateFormat`, `referenceUserFullName`—, asi que `generateHaving` produce contenido en cuanto alguien escribe y la guarda de AP pararia el `having_segment`. Ver T162.",
            "count": 1,
            "salida": "Cuando `HavingSegment` agrupe —v4.1.0 del paquete, sin publicar— este `having_string` puede migrar."
        }
    ]
}
